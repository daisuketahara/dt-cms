{% extends templateservice.getAdminTemplate() %}

{% block content %}
<table class="table table-hover table-striped">
    <thead class="thead-dark">
        <tr>
            <th class="width-100" data-column="id">{% trans %}ID{% endtrans %}</th>
            <th data-column="name">{% trans %}Name{% endtrans %}</th>
            <th data-column="runCount">{% trans %}Run count{% endtrans %}</th>
            <th data-column="lastRun">{% trans %}Last run{% endtrans %}</th>
            <th data-column="active">{% trans %}Active{% endtrans %}</th>
        </tr>
    </thead>
</table>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('js/paginator.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/modal.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/list.js') }}"></script>
    <script type="text/javascript">

        $('.table').dtTable({
            ajaxUrl: '/{{ app.request.locale }}/admin/cron/ajaxlist/',
            addUrl: '/{{ app.request.locale }}/admin/cron/add/',
            editUrl: '/{{ app.request.locale }}/admin/cron/edit/',
            deleteUrl: '/{{ app.request.locale }}/admin/cron/delete/',
            filter: true,
            limit: 15
        });

    </script>
{% endblock %}
